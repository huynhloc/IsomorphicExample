'use strict';

/** 
 * store will pass to reducer 2 arguments
 * state : previous tree app state
 * action: {type: '', data: ''}
 */

const initialState = [];

function posts(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}

module.exports = posts;
